import PropTypes from 'prop-types'
import { requireNativeComponent } from 'react-native';

type WebViewProps = {
    style?: {}
    fname?: string
}
// export const WebView = requireNativeComponent('RCTPDF')
